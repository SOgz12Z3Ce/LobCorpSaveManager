<?xml version="1.0" encoding="utf-8"?>
<Project DefaultTargets="CopyMonoFiles" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <PropertyGroup>
    <LangVersion>3</LangVersion>
    <TargetFrameworkVersion>v3.5</TargetFrameworkVersion>
    <OutputType>Exe</OutputType>
    <AssemblyName>lobcss</AssemblyName>
    <OutputPath>lib\</OutputPath>
    <ThirdPartsPath>third_parts\</ThirdPartsPath>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="Program.cs" />
    <Compile Include="LobCorpSave.cs" />
    <Compile Include="LobCorpSaveSerializer.cs" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="System" />
    <Reference Include="Newtonsoft.Json">
      <HintPath>$(ThirdPartsPath)Newtonsoft.Json.13.0.4\Newtonsoft.Json.dll</HintPath>
    </Reference>
    <MonoFiles Include="$(ThirdPartsPath)Mono-2.8.1\**\*.*" />
  </ItemGroup>

  <Target Name="CopyMonoFiles" DependsOnTargets="Build">
    <Copy
      SourceFiles="@(MonoFiles)"
      DestinationFiles="@(MonoFiles->'$(OutputPath)%(RecursiveDir)%(Filename)%(Extension)')" />
  </Target>

  <Import Project="$(MSBuildToolsPath)\Microsoft.CSharp.targets" />
</Project>
